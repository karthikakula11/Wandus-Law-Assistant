# If the UI shows "Failed to fetch" for uploads/chat, uncomment the next line and
# restart `npm run dev` so the browser calls FastAPI directly (CORS is open on the API).
# VITE_API_BASE=http://127.0.0.1:8000
